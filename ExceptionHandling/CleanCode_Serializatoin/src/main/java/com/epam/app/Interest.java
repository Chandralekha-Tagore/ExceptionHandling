package com.epam.app;
import java.util.*;
public class Interest {
	float simple_interest(float principal_amount,int years,float rate_of_interest)
	{
		return (principal_amount*years*rate_of_interest)/100;
	}
	float compound_interest(float principal_amount,int times_calc,int years,float rate_of_interest)
	{
		return principalamount * Math.pow(1.0 + rate / 100.0 , time) - principal_amount;
	}
	
}
